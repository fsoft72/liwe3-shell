# Configuration for data/cf_ops.js (create / update)
#
# [skip]            paths that are NEVER copied from cf-liwe3-ng into this repo
# [copy-if-missing] paths that are copied ONLY when they do not already exist here
#
# Matching rules:
#   - an entry ending with "/" matches the whole directory (prefix match)
#   - any other entry matches that exact file path
# Blank lines and lines starting with "#" are ignored.

[skip]
CHANGES.md
scripts/data/liwe3-update.conf
pnpm-lock.yaml
pnpm-workspace.yaml
.github/
playground/
PNPM-12.md
README.md
.rafter.yml

[copy-if-missing]
app/
tsconfig.json
package.json
